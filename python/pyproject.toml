[project]
name = "3dwebagent-inspector"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mujoco==3.12.0", "numpy>=2.0", "jsonschema>=4.26"]

[dependency-groups]
dev = [
    "ruff==0.16.6",
]

[tool.ruff.lint]
select = ["F"]
